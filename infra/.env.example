# Copy to infra/.env before starting Docker Compose.
# Generate a long random value; do not commit the real key.
N8N_ENCRYPTION_KEY=replace-with-a-long-random-local-secret

# Optional; only required to exercise Telegram alert delivery.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
